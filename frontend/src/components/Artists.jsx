import React from "react";

export default function Artists(props: any) {
    const {artists} = props;
    console.log(artists);
    return (
        <div>
            {/*{artists.map(a => a.name)}*/}
        </div>
    );
}